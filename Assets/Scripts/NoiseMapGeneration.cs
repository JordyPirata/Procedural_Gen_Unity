using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoiseMapGeneration : MonoBehaviour
{
    public float[,] GenerateNoisemap(int mapWidth, float scale)
    {
        float[,] noiseMap = new float[mapDepth, mapWidth];

        
        
        return noiseMap;
    }
    
        
}
